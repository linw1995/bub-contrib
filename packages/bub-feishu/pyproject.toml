[project]
name = "bub-feishu"
version = "0.1.0"
description = "Feishu channel for Bub"
readme = "README.md"
authors = [
    { name = "Kyle Liu", email = "specter119@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "lark-oapi>=1.4.20",
    "pydantic-settings>=2.10.1",
]

[project.entry-points.bub]
feishu = "bub_feishu.plugin"

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = ["bub_feishu", "skills.feishu"]
source-include = ["src/skills/feishu/SKILL.md", "src/skills/feishu/scripts/*.py"]
